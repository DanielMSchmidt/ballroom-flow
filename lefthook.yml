# Git hooks (docs/system/testing.md § Tooling & CI). Chosen: lefthook — one binary + one YAML replaces
# husky's shell shims AND lint-staged's globbing, runs hooks in parallel, and
# `wrangler`/`pnpm` stay the source of truth. Install with `pnpm install`
# (root `prepare` runs `lefthook install`).
pre-commit:
  parallel: true
  commands:
    # Biome lint+format check on staged JS/TS, then re-stage any autofixes.
    biome:
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
      run: pnpm exec biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    # Type safety. TS project references can't be reliably narrowed to staged
    # files, so typecheck the whole monorepo — it's fast and catches cross-file
    # breakage a staged-only check would miss.
    typecheck:
      run: pnpm -r typecheck

# Block direct pushes to protected branches. This is a fast LOCAL pre-flight;
# GitHub branch protection enforces the same rule on the remote (the real guard).
# If you ever need to bypass intentionally, `git push --no-verify` skips this hook
# (it will NOT bypass the remote protection).
pre-push:
  commands:
    protect-branches:
      run: |
        branch="$(git rev-parse --abbrev-ref HEAD)"
        case "$branch" in
          main|development)
            echo "✋ Direct pushes to '$branch' are blocked — branch off and open a PR."
            exit 1
            ;;
        esac

# `pnpm lint` / `pnpm -r typecheck` / `pnpm -r test` still gate every PR in CI;
# the hook is a fast local pre-flight, not a replacement for CI.
